let arr = window.location.hostname.split('.')
let domain = arr[arr.length - 2] + '.' + arr[arr.length - 1]

export default {
  // cookie domain
  domain: domain,
  // href domain
  // 58coin
  mainDomain: `http://goods-main.test.${domain}/`,
  // account
  userDomain: `http://goods-user.test.${domain}/`,
  // otc
  otcDomain: `http://goods-otc.test.${domain}/`,
  // usdt
  usdtDomain: `http://goods-usdt.test.${domain}/`,
  // 特会平台
  specialDomain: `http://goods-special.test.${domain}/`,
  // api
  registerDomain: `http://goods-register.test.${domain}/`, // 邀请连接
  //baseURL: `http://test-tbex-cfd-admin.cfdtest.${domain}/`, // 主要接口 dev环境
  baseURL: `http://test-tbex-cfd-web-controller.cfdtest.${domain}/`, // 58coin 58spot
  userURL: `http://test-tbex-cfd-web-controller.cfdtest.${domain}/`, // 58coin 58spot
  otcURL: `http://test-tbex-cfd-otc-web.cfdtest.${domain}/cfd/`, // otc
  zendeskURL: 'https://lianbe.zendesk.com/',
  oosImgUrl: `https://tianbi-test.oss-cn-beijing.aliyuncs.com/`
}
